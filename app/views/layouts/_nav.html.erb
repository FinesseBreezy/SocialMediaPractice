
<div class="ui top fixed menu">
  <div class="item">
    <%= link_to '𝒢𝒶𝓏ō', posts_path %>
  </div>
  <% if user_signed_in? %>
  <%= link_to 'Sign Out', destroy_user_session_path, :class => "item", :method => 'delete' %>
  <%= link_to 'Profile', profile_path(current_user.id), :class => "item" %>
  <% else %>
  <%= link_to 'Sign In', new_user_session_path, :class => "item" %>
  <% end %>
  <div class="right menu">
    <div class="item">
      <div class="ui transparent icon input">
        <%= form_tag posts_path, :method => 'get' do%>
        <%= text_field_tag :search, params[:search], :type => "text", :placeholder => "Search"%>
        <%= submit_tag "Search", :class => "search link icon" %>
      </div>
    </div>
  </div>
  <% end %>
</div>
